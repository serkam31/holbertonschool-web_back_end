#!/usr/bin/env python3
"""
This module defines a function `concat` that takes two strings as arguments
and returns their concatenation.
"""


def concat(str1: str, str2: str) -> str:
    """Return a concatenated string."""
    return str1 + str2
