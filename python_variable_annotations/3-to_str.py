#!/usr/bin/env python3
"""
This module defines a function `to_str` that takes a float as an argument
and returns its string representation.
"""


def to_str(n: float) -> str:
    """Return the string representation of a float."""
    return str(n)
